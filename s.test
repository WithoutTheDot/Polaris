# Factorial
VAR a
FUN factorial(n)
    IF n == 0 THEN
        RETURN 1
    ELSE
        RETURN n*factorial(n-1)
    END
END

VAR s = TIME()
FOR i=0 TO 500 DO
    PRINT("The factorial of "+ STR(i)+" is "+ STR(factorial(i)))
END
VAR e = TIME()
PRINT("Executed in "+ STR(e-s)+" seconds")
